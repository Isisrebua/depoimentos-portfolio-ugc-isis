SUPABASE_URL=
SUPABASE_KEY=

# Senha do painel /crm e /admin. Opcional — se não definida, usa o valor
# hardcoded em lib/crm-auth.js (mantido só por compatibilidade). Defina
# aqui (e nas env vars do Render/Vercel) pra trocar a senha sem editar código.
CRM_PASSWORD=
